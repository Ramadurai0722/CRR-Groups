/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // Removed to enable Netlify Next.js Runtime optimizations
  // images: { unoptimized: true } // Removed to enable automatic image optimization
};

export default nextConfig;
